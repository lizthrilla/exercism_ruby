class Hamming
  def self.compute(a, b)
    #basic comparison test to check ruby syntax
    raise ArgumentError if a.length != b.length
    if a == b
      0
    end
    #look up .count and .each_char string functions
  end
end
